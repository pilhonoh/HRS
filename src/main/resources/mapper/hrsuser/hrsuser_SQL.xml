<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hrsuserMapper">

	<select id="hrsuserMapper.selectUserInfo" parameterType="map" resultType="hashmap">
		SELECT A.EMPNO,
			A.HNAME,
		    CASE WHEN A.T_SEX = '2' THEN 'F' ELSE 'M' END AS T_SEX,
		    A.PLACE,
		    B.AUTH_CODE,
		    B.BLD_CODE
		FROM inf_person_info_rcv A
		LEFT JOIN CHARGER B
		ON A.EMPNO = B.EMPNO
		WHERE A.EMPNO = #{empno}
	</select>
	
</mapper>